# SMTP configuration for sending booking notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=info.sassolar@gmail.com
# For Gmail, use an App Password (not your primary password)
SMTP_PASS=
# The email address that will receive the booking notification
NOTIFICATION_EMAIL=info.sassolar@gmail.com

# Supabase configuration for client-side storage and database
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
